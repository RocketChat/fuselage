export function mrkdwn({ text /* , type = 'plain_text'*/ } = { text: '' }) {
  return text;
}

export function plainText({ text /* , type = 'plain_text'*/ } = { text: '' }) {
  return text;
}

export function text({ text /* , type = 'plain_text'*/ } = { text: '' }) {
  return text;
}
