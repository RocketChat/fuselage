import transitions from '../tokens/transitions';
import { varTheme } from '../helpers/varTheme';

export default {
  shortDuration: varTheme('transitions', 'shortDuration', transitions.shortDuration),
};
