import type { HTMLAttributes } from 'react';
import React from 'react';

export const SideBarSection = (props: HTMLAttributes<HTMLDivElement>) => (
  <div className='rcx-sidebar-v2-section' {...props} />
);
