import {useRef} from 'react';
import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { Box, Button, Position, Options } from '../..';
const options = [
  [1, 'a teste 1'],
  [2, 'b teste 2'],
  [3, 'c teste 3', true],
  [4, 'd teste 4'],
];

<Meta title='Box|Positioning' parameters={{ jest: ['Box/spec'] }} />

# Box

### Text styles

<Preview>
  <Story name='Default'>
    {(() => {
      const ref = useRef();
      return (<>
        <Button style={{margin:'100px', width: '100%', maxWidth:'500px'}}ref={ref}>Anchor test</Button>
        <Position width='auto' placement='bottom right' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='bottom left' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='bottom center' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='top right' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='top left' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='top center' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='center right' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='center left' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
        <Position width='auto' placement='center center' anchor={ref}><Options options={options} value={[1]} cursor={0}/></Position>
      </>);
    })()}
  </Story>
</Preview>
