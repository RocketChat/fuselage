import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { Box, Button } from '../..';

<Meta title='Box' parameters={{ jest: ['Box/spec'] }} />

# Box

A primitive component with normalized styles.

<Props of={Box} />

### `is` prop

The `is` prop allows `Box` to render any component capable of handling common DOM attributes, specially `className` and
`style`.

<Preview>
  <Story name='is prop'>
    <Box is={Button}>A Box rendering a Button</Box>
  </Story>
</Preview>

### `invisible` prop

The `Box` can be hidden using the boolean prop `invisible`.

<Preview>
  <Story name='invisible prop'>
    <>
      <Box>Visible</Box>
      <Box invisible>Invisible</Box>
    </>
  </Story>
</Preview>

### `rcx-` prefixed props

As the class names used in Fuselage are prefixed by `rcx-`, props named as `rcx-*` are automatically converted into
values appended to the `className` prop.

For instance,

```jsx
  <Box rcx-component rcx-component--bool-modifier rcx-component--string-modifier='value' />
```

is rendered as

```html
  <div class="rcx-box rcx-component rcx-component--bool-modifier rcx-component--string-modifier-value"></div>
```

### [DEPRECATED] `componentClassName` prop and modifier props

Following the [BEM convention](http://getbem.com/), `Box` elements can have a `componentClassName` comprising the block
and element parts while the modifiers are appended via props prefixed with `mod-`. The generated class name will be
appended to the `className` prop value and `rcx-box` class names coming from components named `Box` modifiers.

For instance,

```jsx
  <Box componentClassName='my-component' mod-bool-modifier mod-string-modifier='value' />
```

is rendered as

```html
  <div class="rcx-box my-component my-component--bool-modifier my-component--string-modifier-value"></div>
```
