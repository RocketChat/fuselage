import { action } from '@storybook/addon-actions';
import { Meta, Canvas, ArgsTable, Story } from '@storybook/addon-docs';

import { CheckBox } from '../..';
import { PropsVariationSection } from '../../../.storybook/helpers';

<Meta title='Buttons/CheckBox' parameters={{ jest: ['CheckBox/spec'] }} />

# CheckBox

<Canvas>
  <Story name='Default'>
    <CheckBox onChange={action('change')} />
  </Story>
</Canvas>

<ArgsTable of={CheckBox} />

## Checked

<Canvas>
  <Story name='Checked'>
    <CheckBox checked onChange={action('change')} />
  </Story>
</Canvas>

## Indeterminate

<Canvas>
  <Story name='Indeterminate'>
    <CheckBox indeterminate onChange={action('change')} />
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name='Disabled'>
    <CheckBox disabled />
  </Story>
</Canvas>

## States

<Story name='States'>
  <PropsVariationSection
    component={CheckBox}
    common={{ onChange: action('change') }}
    xAxis={{
      checked: { checked: true },
      unchecked: { checked: false },
      indeterminate: { indeterminate: true },
    }}
    yAxis={{
      default: {},
      hover: { className: 'is-hovered' },
      active: { className: 'is-active' },
      focus: { className: 'is-focused' },
      disabled: { disabled: true },
    }}
  />
</Story>
