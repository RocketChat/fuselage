import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';

import { Grid, Tile } from '../..';

<Meta title='Containers|Grid' parameters={{ jest: ['Grid/spec'] }} />

# Grid

<Preview>
  <Story name='Default'>
    <Grid>
      <Grid.Item>
        <Tile elevation={1} />
      </Grid.Item>
      <Grid.Item>
        <Tile elevation={1} />
      </Grid.Item>
      <Grid.Item>
        <Tile elevation={1} />
      </Grid.Item>
    </Grid>
  </Story>
</Preview>

### Grid

<Props of={Grid} />

### Grid.Item

<Props of={Grid.Item} />

## Breakpoints

|                   | xsmall | small | medium | large  | xlarge |
|-------------------|--------|-------|--------|--------|--------|
| Alias             | xs     | sm    | md     | lg     | xl     |
| Minimum width     | 320px  | 600px | 768px  | 1024px | 1440px |
| Columns           | 4      | 8     | 8      | 12     | 12     |
| Margins / Gutters | 16px   | 16px  | 24px   | 24px   | 24px   |

### Extra Small (xs)

<Story name='xs breakpoint'>
  <Grid>
    {Array.from({ length: 4 }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid.Item xs={columns}>
          <Tile elevation={1} style={{ padding: 16 }}>xs={columns}</Tile>
        </Grid.Item>
        {(4 - columns > 0) &&
        <Grid.Item xs={4 - columns}>
          <Tile elevation={1} style={{ padding: 16 }}>xs={4 - columns}</Tile>
        </Grid.Item>}
      </React.Fragment>
    )}
  </Grid>
</Story>

### Small (sm)

<Story name='sm breakpoint'>
  <Grid>
    {Array.from({ length: 8 }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid.Item sm={columns}>
          <Tile elevation={1} style={{ padding: 16 }}>sm={columns}</Tile>
        </Grid.Item>
        {(8 - columns > 0) &&
        <Grid.Item sm={8 - columns}>
          <Tile elevation={1} style={{ padding: 16 }}>sm={8 - columns}</Tile>
        </Grid.Item>}
      </React.Fragment>
    )}
  </Grid>
</Story>

### Medium (md)

<Story name='md breakpoint'>
  <Grid>
    {Array.from({ length: 8 }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid.Item md={columns}>
          <Tile elevation={1} style={{ padding: 16 }}>md={columns}</Tile>
        </Grid.Item>
        {(8 - columns > 0) &&
        <Grid.Item md={8 - columns}>
          <Tile elevation={1} style={{ padding: 16 }}>md={8 - columns}</Tile>
        </Grid.Item>}
      </React.Fragment>
    )}
  </Grid>
</Story>

### Large (lg)

<Story name='lg breakpoint'>
  <Grid>
    {Array.from({ length: 12 }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid.Item lg={columns}>
          <Tile elevation={1} style={{ padding: 16 }}>lg={columns}</Tile>
        </Grid.Item>
        {(12 - columns > 0) &&
        <Grid.Item lg={12 - columns}>
          <Tile elevation={1} style={{ padding: 16 }}>lg={12 - columns}</Tile>
        </Grid.Item>}
      </React.Fragment>
    )}
  </Grid>
</Story>

### Extra Large (xl)

<Story name='xl breakpoint'>
  <Grid>
    {Array.from({ length: 12 }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid.Item xl={columns}>
          <Tile elevation={1} style={{ padding: 16 }}>xl={columns}</Tile>
        </Grid.Item>
        {(12 - columns > 0) &&
        <Grid.Item xl={12 - columns}>
          <Tile elevation={1} style={{ padding: 16 }}>xl={12 - columns}</Tile>
        </Grid.Item>}
      </React.Fragment>
    )}
  </Grid>
</Story>

