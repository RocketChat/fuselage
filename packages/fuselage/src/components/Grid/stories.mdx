import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';

import breakpoints from '../../styles/variables/breakpoints';
import { Grid, Tile } from '../..';

<Meta title='Containers|Grid' parameters={{ jest: ['Grid/spec'] }} />

# Grid

<Preview>
  <Story name='Default'>
    <Grid>
      <Grid.Item>
        <Tile elevation={1} />
      </Grid.Item>
      <Grid.Item>
        <Tile elevation={1} />
      </Grid.Item>
      <Grid.Item>
        <Tile elevation={1} />
      </Grid.Item>
    </Grid>
  </Story>
</Preview>

### Grid

<Props of={Grid} />

### Grid.Item

<Props of={Grid.Item} />

## Breakpoints

### Extra Small (xs)

<Story name='xs breakpoint'>
  <>
    {Array.from({ length: breakpoints.xs.columns }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid>
          <Grid.Item xs={columns}>
            <Tile children={columns} elevation={1} style={{ padding: 16 }}>xs={columns}</Tile>
          </Grid.Item>
        </Grid>
        <br />
      </React.Fragment>
    )}
  </>
</Story>

### Small (sm)

<Story name='sm breakpoint'>
  <>
    {Array.from({ length: breakpoints.sm.columns }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid>
          <Grid.Item sm={columns}>
            <Tile children={columns} elevation={1} style={{ padding: 16 }}>sm={columns}</Tile>
          </Grid.Item>
        </Grid>
        <br />
      </React.Fragment>
    )}
  </>
</Story>

### Medium (md)

<Story name='md breakpoint'>
  <>
    {Array.from({ length: breakpoints.md.columns }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid>
          <Grid.Item md={columns}>
            <Tile children={columns} elevation={1} style={{ padding: 16 }}>md={columns}</Tile>
          </Grid.Item>
        </Grid>
        <br />
      </React.Fragment>
    )}
  </>
</Story>

### Large (lg)

<Story name='lg breakpoint'>
  <>
    {Array.from({ length: breakpoints.lg.columns }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid>
          <Grid.Item lg={columns}>
            <Tile children={columns} elevation={1} style={{ padding: 16 }}>lg={columns}</Tile>
          </Grid.Item>
        </Grid>
        <br />
      </React.Fragment>
    )}
  </>
</Story>

### Extra Large (xl)

<Story name='xl breakpoint'>
  <>
    {Array.from({ length: breakpoints.xl.columns }).map((_, i) => i + 1).map((columns) =>
      <React.Fragment key={columns}>
        <Grid>
          <Grid.Item xl={columns}>
            <Tile elevation={1} style={{ padding: 16 }}>xl={columns}</Tile>
          </Grid.Item>
        </Grid>
        <br />
      </React.Fragment>
    )}
  </>
</Story>

