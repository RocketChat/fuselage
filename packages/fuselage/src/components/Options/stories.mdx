import { action } from '@storybook/addon-actions';
import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';
import { PropsVariationSection } from '../../../.storybook/helpers';
import { Box } from '../'
import { Options, CheckOption, OptionAvatar } from './';
const thumb = 'data:image/gif;base64,R0lGODlhAQABAIAAAMLCwgAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==';
const options = [
  [1, 'a teste 1'],
  [2, 'b teste 2'],
  [3, 'c teste 3', true],
  [4, 'd teste 4'],
];

<Meta title='Misc|Options' parameters={{ jest: ['Options/spec'] }} />

# Options

An input for selection of options.

<Preview>
  <Story name='Default'>
    <Box style={{ position: 'relative', maxWidth: 250 }} >
      <Options options={options}  cursor={1} />
    </Box>
  </Story>
</Preview>

<Props of={Options} />

<Preview>
  <Story name='CheckOption'>
    <Box style={{ position: 'relative', maxWidth: 250 }} >
      <Options renderItem={CheckOption} options={options} value={[1]} cursor={0}/>
    </Box>
  </Story>
</Preview>

<Preview>
  <Story name='OptionAvatar'>
    <Box style={{ position: 'relative', maxWidth: 250 }} >
      <Options renderItem={(props) => <OptionAvatar {...props} value={thumb}/>}  options={options} />
    </Box>
  </Story>
</Preview>
