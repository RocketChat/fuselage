import { Meta, Canvas, ArgsTable, Story } from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';

import { Icon, PasswordInput } from '../..';
import { PropsVariationSection } from '../../../.storybook/helpers';

<Meta
  title='Forms/Inputs/PasswordInput'
  parameters={{ jest: ['PasswordInput/spec'] }}
/>

# PasswordInput

An input for passwords with toggle.

<Canvas>
  <Story name='Default'>
    <PasswordInput />
  </Story>
</Canvas>

<ArgsTable of={PasswordInput} />

## Props from <LinkTo kind='Forms/InputBox' story='Default Story'>`InputBox`</LinkTo>

Aside from `type`, any other prop from <LinkTo kind='Forms/InputBox' story='Default Story'>`InputBox`</LinkTo> can be
used.

### With Value

<Canvas>
  <Story name='With Value'>
    <PasswordInput defaultValue='password'/>
  </Story>
</Canvas>

### With Icon Addon

<Canvas>
  <Story name='With Icon Addon'>
    <PasswordInput addon={<Icon name='send' size='x20' />} />
  </Story>
</Canvas>

### Invalid

<Canvas>
  <Story name='Invalid'>
    <PasswordInput error='Error' />
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name='Disabled'>
    <PasswordInput disabled />
  </Story>
</Canvas>

### With placeholder

<Canvas>
  <Story name='With Placeholder'>
    <PasswordInput placeholder='Placeholder' />
  </Story>
</Canvas>

## States

<Story name='States'>
  <PropsVariationSection
    component={PasswordInput}
    common={{ onChange: () => {} }}
    xAxis={{
      'default': {},
      'with placeholder': { placeholder: 'Placeholder' },
      'with value': { value: 'Value' },
      'with icon': { addon: <Icon name='key' size='x20' />, value: 'Value' },
    }}
    yAxis={{
      'default': {},
      'hover': { className: 'hover' },
      'active': { className: 'active' },
      'focus': { className: 'focus' },
      'disabled': { disabled: true },
      'errored': { error: 'Error' },
      'errored + hover': { className: 'hover', error: 'Error' },
      'errored + active': { className: 'active', error: 'Error' },
      'errored + focus': { className: 'focus', error: 'Error' },
    }}
  />
</Story>
