import { action } from '@storybook/addon-actions';
import { Meta, Canvas, ArgsTable, Story } from '@storybook/addon-docs/blocks';

import { Pagination } from '../..';

<Meta title='Data Display/Pagination' parameters={{ jest: ['Pagination/spec'] }} />

# Pagination

<Canvas>
  <Story name='Default'>
    <Pagination
      count={500}
      onSetItemsPerPage={action('setItemsPerPage')}
      onSetCurrent={action('setCurrent')}
    />
  </Story>
</Canvas>


<Canvas>
  <Story name='Divider'>
    <Pagination
      divider
      count={500}
      current={50}
      onSetItemsPerPage={action('setItemsPerPage')}
      onSetCurrent={action('setCurrent')}
    />
  </Story>
</Canvas>

<ArgsTable of={Pagination} />
