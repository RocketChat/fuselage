import { action } from '@storybook/addon-actions';
import { Meta, Canvas, ArgsTable, Story } from '@storybook/addon-docs/blocks';
import { useState } from 'react';

import { Select, MultiSelect, MultiSelectFiltered, SelectFiltered } from '.';
import { options } from './hugeList';

export const thumb =
  'data:image/gif;base64,R0lGODlhAQABAIAAAMLCwgAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==';

export const optionsEllipses = [
  [11, 'Very very very very very very very very very large text'],
  ...options,
];

<Meta
  title='Forms/Inputs/SelectPaginated'
  parameters={{ jest: ['SelectPaginated/spec'] }}
/>

# Select

An input for selection of options.

<Canvas>
  <Story name='Default'>
    <Select width='250px' placeholder='Placeholder here...' options={options} />
  </Story>
</Canvas>

<Canvas>
  <Story name='Error'>
    <Select
      width='250px'
      error
      placeholder='Placeholder here...'
      options={options}
    />
  </Story>
</Canvas>

<Canvas>
  <Story name='Disabled'>
    <Select
      width='250px'
      disabled
      placeholder='Placeholder here...'
      options={options}
    />
  </Story>
</Canvas>

#### No Placeholder

<Canvas>
  <Story name='No Placeholder'>
    <Select width='250px' options={options} />
  </Story>
</Canvas>

<ArgsTable of={Select} />

# Select with filter

An input for selection of options.

<Canvas>
  <Story name='Select with filter'>
    <SelectFiltered
      width='250px'
      placeholder='Placeholder here...'
      options={options}
    />
  </Story>
</Canvas>

<Canvas>
  <Story name='Select with filter and ellipses'>
    <SelectFiltered
      width='250px'
      value={11}
      placeholder='Placeholder here...'
      options={optionsEllipses}
    />
  </Story>
</Canvas>

<Canvas>
  <Story name='Select with filter and error'>
    <SelectFiltered
      width='250px'
      error
      placeholder='Placeholder here...'
      options={options}
    />
  </Story>
</Canvas>

<Canvas>
  <Story name='Select with filter and disabled'>
    <SelectFiltered
      width='250px'
      disabled
      error
      placeholder='Placeholder here...'
      options={options}
    />
  </Story>
</Canvas>

<ArgsTable of={SelectFiltered} />

# MultiSelect

An input for selection of options.

<Canvas>
  <Story name='MultiSelect'>
    <MultiSelect placeholder='Placeholder here...' options={options} />
  </Story>
</Canvas>

<Canvas>
  <Story name='MultiSelect Error'>
    <MultiSelect error placeholder='Placeholder here...' options={options} />
  </Story>
</Canvas>

<Canvas>
  <Story name='MultiSelect Disabled'>
    <MultiSelect disabled placeholder='Placeholder here...' options={options} />
  </Story>
</Canvas>

<ArgsTable of={MultiSelect} />

# MultiSelectFiltered

An input for selection of options.

<Canvas>
  <Story name='MultiSelectFiltered'>
    {React.createElement(() => {
      const [filter, setFilter] = useState('');
      return (
        <MultiSelectFiltered
          placeholder='Placeholder here...'
          onChange={action('change')}
          options={options}
          filter={filter}
          setFilter={setFilter}
        />
      );
    })}
  </Story>
</Canvas>

<Canvas>
  <Story name='MultiSelectFiltered error'>
    <MultiSelectFiltered
      error
      placeholder='Placeholder here...'
      onChange={action('change')}
      options={options}
    />
  </Story>
</Canvas>

<Canvas>
  <Story name='MultiSelectFiltered disabled'>
    <MultiSelectFiltered
      disabled
      placeholder='Placeholder here...'
      onChange={action('change')}
      options={options}
    />
  </Story>
</Canvas>

<ArgsTable of={MultiSelectFiltered} />
