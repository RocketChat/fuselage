import {useRef} from 'react';
import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { Modal } from '.';
import { ModalWrapper } from './Stack';
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs";
import { ButtonGroup, Button, AnimatedWrapper } from '../..'
const thumb = 'data:image/gif;base64,R0lGODlhAQABAIAAAMLCwgAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==';

<Meta title='Modal' parameters={{ jest: ['Modal/spec'] }} decorators={[withKnobs]}/>

# Box

### Text styles

<Preview>
  <Story name='Default'>
    <AnimatedWrapper visible={boolean('Visible', true) ? 1 : 0}>
      <Modal>
        <Modal.Header><Modal.Thumb url={thumb}/> <Modal.Title>Modal Header</Modal.Title><Modal.Close/></Modal.Header>
        <Modal.Container>Modal Body</Modal.Container>
        <Modal.Footer>
          <ButtonGroup align='end'>
            <Button>Cancel</Button>
            <Button primary>Submit</Button>
          </ButtonGroup>
        </Modal.Footer>
      </Modal>
    </AnimatedWrapper>
  </Story>
</Preview>

<Preview>
  <Story name='Stack'>
    <ModalWrapper/>
  </Story>
</Preview>
