import { action } from '@storybook/addon-actions';
import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';

import { PropsVariationSection } from '../../../.storybook/helpers';
import { Icon, AutoComplete, Chip, Select, MultiSelectFiltered } from '../..';
const thumb = 'data:image/gif;base64,R0lGODlhAQABAIAAAMLCwgAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==';
const options = [
  [1, 'a teste 1'],
  [2, 'b teste 2'],
  [3, 'c teste 3'],
  [4, 'd teste 4'],
];


<Meta title='Forms|Inputs/AutoComplete' parameters={{ jest: ['AutoComplete/spec'] }} />

# AutoComplete

An input for selection of options.

<Preview>
  <Story name='Default'>
    <AutoComplete placeholder="Add users...">
      <Chip onClick={console.log} thumbUrl={thumb}>John</Chip>
    </AutoComplete>
  </Story>
</Preview>

# Select

An input for selection of options.

<Preview>
  <Story name='Select'>
    <Select placeholder="placeholder here..." options={options}/>
  </Story>
</Preview>

<Props of={AutoComplete} />

# Select with filter

An input for selection of options.

<Preview>
  <Story name='Select with filter'>
    <Select placeholder="placeholder here..." options={options}/>
  </Story>
</Preview>

<Props of={AutoComplete} />

# MultiSelect

An input for selection of options.

<Preview>
  <Story name='MultiSelect'>
    <Select placeholder="placeholder here..." options={options}/>
  </Story>
</Preview>


# MultiSelectFiltered

An input for selection of options.

<Preview>
  <Story name='MultiSelectFiltered'>
    <MultiSelectFiltered placeholder="placeholder here..." options={options}/>
  </Story>
</Preview>

<Props of={AutoComplete} />
