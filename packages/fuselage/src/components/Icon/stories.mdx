import namesToCharactersMapping from '@rocket.chat/icons';
import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';

import { PropsVariationSection } from '../../../.storybook/helpers';
import { Hint, Icon, Text } from '../..';

<Meta title='Misc|Icon' />

# Icon

<Story name='Default'>
  <Text is='div' paragraph defaultColor>
    {Object.keys(namesToCharactersMapping).map((name) => <Icon key={name} name={name} size='40' /> )}
  </Text>
</Story>

<Props of={Icon} />

export const styles = {
  container: {
    display: 'flex',
    flexFlow: 'row wrap',
    alignItems: 'center',
    justifyContent: 'space-between',
    textAlign: 'center',
  },
  wrapper: {
    margin: '2rem 0.5rem',
    flex: '1 0 8rem',
  },
};

## Available icons

<div style={styles.container}>
  {Object.keys(namesToCharactersMapping).map((name) =>
    <div key={name} style={styles.wrapper}>
      <Text is='div'>
        <Icon name={name} size='40' />
      </Text>
      <Text is='div' hintColor>{name}</Text>
    </div>
  )}
</div>
