---
"@rocket.chat/fuselage": minor
---

feat: Visual Regression Testing
  Introduces a new tool package `visual-regression` to be used for easily enabling of visual regression testing across the monorepo packages.
  Enables visual regression testing for `fuselage` package.
  Adds a dedicated CI step for running visual regressions across all monorepo packages (that have it enabled).
