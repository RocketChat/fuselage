---
'@rocket.chat/fuselage': minor
---

feat(fuselage)!: Remove the `ghost` variant and the `disabled` prop from `Badge`

The `ghost` variant was discontinued in favor of `secondary` and no longer exists in the design system; use `secondary` instead. The `disabled` visual state was removed along with it.
