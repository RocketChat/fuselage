name: Loki
branding:
  icon: "check"
  color: "green"
inputs:
  working_dir:
    required: true
    default: .
  react_uri:
    required: true
    default: ./storybook-static
outputs:  
  regex:
    description: A regex of components to run test against
runs:
  using: "node20"
  main: dist/index.js